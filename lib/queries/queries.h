#ifndef QUERIES_H
#define QUERIES_H

void leeQuery(Estudiante arr[], bool write, char *filename, bool verbose);
void ejecutaQuery(Estudiante arr[], char *query, bool write, char *filename, bool verbose);

#endif